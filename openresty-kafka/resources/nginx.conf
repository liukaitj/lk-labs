worker_processes  1;
error_log logs/error.log;
events {
    worker_connections 1024;
}
http {
    server {
        listen 8080;
        location /report {
            lua_code_cache on;
            log_by_lua_file /usr/local/openresty/lualib/app/report.lua;
        }
    }
}